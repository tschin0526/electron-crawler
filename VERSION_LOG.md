# 📌 无限空间·AI智控台 - 版本更新日志

## 版本号格式
`主版本.次版本.修订-修改序号`

- **修改序号**：每次功能修改 +1
- **主/次/修订**：重大架构变更时递增

---

## v1.0.0-45 🎯 识别后台服务与综合优化（当前版本）

### 📅 日期：2026-07-30
### 🔧 主要变更：
1. **识别后台服务**：新增 `lastUsedServiceCard` 状态共享，API `/api/current-service-card` 可返回当前使用的服务卡片。
2. **浮动视窗增强**：消息文字支持点击展开/折叠，显示 ▼/▲ 箭头；卡片与通用页签行为一致。
3. **历史消息下拉**：所有网络卡片输入框支持双击和 `?` 键打开历史消息下拉。
4. **邮件插件重构**：改为「邮件发送 / 发送历史」页签布局，全页面空间利用，支持清空历史。
5. **排程优化**：执行指令输入框改为六行高度，便于编辑长指令。
6. **版本号统一**：`APP_VERSION` 维护在 `main.js`，同步到窗口标题、API 响应与日志。

### 📁 涉及文件：
- `main.js`
- `main-modules/api-server.js`
- `main-modules/ipc-storage.js`
- `src/modules/scheduler.js`
- `src/modules/bookmarks.js`
- `src/modules/floating-window.js`
- `src/crawler-window.html`
- `src/plugins/email/index.html`

---

## v1.0.0-18 💾 注入模式持久化存储修复

### 📅 日期：2026-06-15
### 🔧 修改内容：
1. **修复消息持久化** - 注入模式 AI 回复现在会保存到 AIChatDB 本地数据库
2. **添加折叠状态处理** - 与直接发送模式一致，最新3条展开其他折叠
3. **版本号升级** - v1.0.0-17 → v1.0.0-18

### 🐛 修复的问题：
- ❌ 刷新页面后注入模式的 AI 回复消失
- ✅ 现在刷新后所有消息都能正确保留

### 📝 关键代码改动：
- `aiChatParserPlugin.js`: 注入模式添加 `setMessages` 折叠状态处理
- `aiChatParserPlugin.js`: 注入模式添加 `window.AIChatDB.add()` 持久化存储
- `main.js`: 版本号更新为 `1.0.0-18`
- `crawler-window.html`: 标题版本号更新

---

## v1.0.0-17 ✨ 注入模式完整监控版

### 📅 日期：2026-06-15
### 🔧 修改内容：
1. **注入模式完整监控** - 复用 `captureElementContentByIndex()` 的心跳监控+稳定检测逻辑
2. **传入 serviceCardIndex** - 前端传入卡片索引，后端使用完整监控函数
3. **版本号系统** - 新增 APP_VERSION 常量，在窗口标题/API响应/日志中显示

### 📝 关键代码改动：
- `main.js`: 添加 `APP_VERSION = '1.0.0-17'`
- `main.js`: 窗口标题显示版本号
- `main.js`: API 启动日志显示版本号
- `main.js`: 注入 API 请求/响应包含版本号
- `aiChatParserPlugin.js`: 前端传入 serviceCardIndex

---

## v1.0.0-16 💉 注入模式基础功能

### 📅 日期：2026-06-15
### 🔧 修改内容：
1. **注入消息到输入框** - 使用 native setter 安全注入文本
2. **自动点击发送按钮** - 多选择器兼容
3. **字符串拼接修复** - 避免模板字符串嵌套转义问题

---

## v1.0.0-15 🎯 AI 对话上下文管理（大模型模式）

### 📅 日期：2026-06-15
### 🔧 修改内容：
1. **多轮对话支持** - 收集历史消息发送给 AI API
2. **三种模型适配** - 智谱/方舟/通义千问
3. **消息格式转换** - 前端状态 → API 格式

---

## v1.0.0-14 🔄 直接发送模式优化

### 📅 日期：2026-06-15
### 🔧 修改内容：
1. **统一 Preview 路径** - 所有服务卡片使用预览模式
2. **参数传递增强** - replySelector/heartbeatSelector/monitorTimeout 从前端传入
3. **错误处理完善** - 详细错误信息返回

---

## v1.0.0-13 🌐 网页活动监控

### 📅 日期：2026-06-15
### 🔧 修改内容：
1. **waitForWebActivityToSettle()** - 心跳监控 + 稳定检测
2. **MutationObserver** - DOM 变化监听
3. **超时机制** - monitorTimeout 参数控制等待时间

---

## v1.0.0-12 📸 元素内容抓取

### 📅 日期：2026-06-15
### 🔧 修改内容：
1. **captureElementContentByIndex()** - 选择器抓取元素内容
2. **智能元素选择** - 找最长可见元素
3. **HTML + 文本双输出** - 支持 Markdown 渲染

---

## v1.0.0-11 🔐 IPC 通信接口

### 📅 日期：2026-06-15
### 🔧 修改内容：
1. **preload.js** - contextIsolation 安全通信
2. **IPC 主进程处理** - 智控/预览/抓取等操作
3. **HTTP API 服务器** - 端口 3000 供外部调用

---

## v1.0.0-10 🕷️ Electron 基础框架

### 📅 日期：2026-06-15
### 🔧 修改内容：
1. **BrowserWindow 创建** - 禁用 Web 安全限制
2. **webview 标签支持** - 内嵌网页浏览
3. **多窗口管理** - 主窗口/元素查看器/DOM树/AI回复查看器

---

## 📋 版本号使用规范

### 何时递增修改序号：
- ✅ 修复 Bug
- ✅ 添加新功能
- ✅ 优化现有逻辑
- ✅ 修改配置或参数

### 何时递增修订号：
- ✅ 重构某个模块
- ✅ 移除废弃功能
- ✅ 性能优化

### 何时递增次版本号：
- ✅ 新增重要功能模块
- ✅ API 接口变更
- ✅ 数据结构变化

### 何时递增主版本号：
- ✅ 架构重构
- ✅ 不兼容的 API 变更
- ✅ 核心功能重写

---

## 🔍 如何确认版本

### 方法 1：查看窗口标题
```
🌌 无限空间·AI智控台 v1.0.0-17
```

### 方法 2：查看 Console 日志
```
╔══════════════════════════════════════════════════╗
║  无限空间·AI智控台                              ║
║  版本: 1.0.0-17                              ║
╚══════════════════════════════════════════════════╝
```

### 方法 3：API 响应
```json
{
  "success": true,
  "version": "1.0.0-17",
  ...
}
```

### 方法 4：API 请求日志
```
📌 [Main] [API] 🔐 当前运行版本: v1.0.0-17
📌 [Main] [API] 🔐 函数执行版本: v1.0.0-17
```

---

## ⚠️ 重要提示

**每次修改代码后：**
1. ✏️ 更新此文件的版本记录
2. 🔢 递增 `APP_VERSION` 的修改序号
3. 📝 简要描述本次修改内容
4. 💾 保存并重启 Electron
5. 📸 截图给 AI 助手看版本号确认

**这样就能 100% 确认是否加载了最新代码！**
