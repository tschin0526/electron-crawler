# 🌌 无限空间·AI智控台 - 完整使用指南

## 📋 目录

1. [快速启动](#快速启动)
2. [使用步骤](#使用步骤)
3. [与网页版配合](#与网页版配合)
4. [常见问题](#常见问题)
5. [最佳实践](#最佳实践)

---

##  快速启动

### 方法一：一键启动脚本（最简单）

打开终端，运行：

```bash
/Users/chincharles/Desktop/myProgram/electron-crawler/start.sh
```

或者在终端中直接输入：

```bash
sh /Users/chincharles/Desktop/myProgram/electron-crawler/start.sh
```

### 方法二：使用 npm 命令

```bash
cd /Users/chincharles/Desktop/myProgram/electron-crawler
npm start
```

### 方法三：创建桌面快捷方式（推荐）

#### 在 macOS 上：

1. 打开「Automator」（自动操作）应用
2. 选择「应用程序」
3. 添加「运行 Shell 脚本」
4. 输入以下内容：
   ```bash
   cd /Users/chincharles/Desktop/myProgram/electron-crawler
   npm start
   ```
5. 保存为「无限空间·AI智控台」
6. 现在可以在 Launchpad 或应用程序文件夹中找到它

#### 在桌面上创建快捷方式：

```bash
# 创建桌面快捷方式
ln -s /Users/chincharles/Desktop/myProgram/electron-crawler/start.sh ~/Desktop/AI智控台.command
```

然后双击桌面上的「AI智控台.command」即可启动。

---

## 📖 使用步骤

### 1. 启动应用

使用上述任一方法启动无限空间·AI智控台应用。

### 2. 输入网址

在输入框中输入要访问的网址，例如：

```
https://baike.baidu.com/item/大同市/529184
https://www.wikipedia.org
https://www.google.com/search?q=人工智能
```

### 3. 开始爬取

点击「🎯 开始爬取」按钮，等待爬取完成。

### 4. 查看数据

爬取完成后，可以切换不同的标签查看数据：

- **📋 超链接** - 页面中的所有链接
- **📊 表格** - 页面中的所有表格
- ** 文本** - 页面的全部文本内容
- **🖼️ 图片** - 页面中的所有图片
- **🔘 按钮** - 页面中的所有按钮

### 5. 导出数据

点击底部按钮导出数据：

- **📥 导出 JSON** - 导出完整的结构化数据
- **📊 导出 CSV** - 导出链接数据为 CSV 格式

---

##  与网页版配合

你现在有三个智控工具，可以配合使用：

### 工具对比

| 工具 | 位置 | 优势 | 适用场景 |
|------|------|------|---------|
| **AI智控台** | 独立窗口 | 无跨域限制 | 访问任何外部网站 |
| **智控实验室** | 主程序浮动窗 | 访问当前页 | 访问已打开的页面 |
| **全网搜索 API** | 主程序浮动窗 | 生成搜索链接 | 快速生成多个搜索链接 |

### 推荐工作流程

#### 流程一：直接访问

```
1. 启动无限空间·AI智控台应用
2. 输入目标网址
3. 点击「开始访问」
4. 导出数据
```

#### 流程二：搜索 + 爬取

```
1. 在主程序中打开「全网搜索 API 实验室」
2. 输入关键词，生成搜索链接
3. 在浏览器中打开搜索结果
4. 复制目标网址
5. 在 AI智控台中粘贴并访问
6. 导出数据
```

#### 流程三：当前页访问

```
1. 在浏览器中打开目标页面
2. 在主程序中打开「智控实验室」
3. 勾选「访问当前页面」
4. 点击「开始访问」
```

---

## ❓ 常见问题

### Q1: 应用启动失败？

**解决方法：**

```bash
# 1. 检查 Node.js 是否安装
node --version

# 2. 重新安装依赖
cd /Users/chincharles/Desktop/myProgram/electron-crawler
rm -rf node_modules package-lock.json
npm install --registry=https://registry.npmmirror.com

# 3. 再次启动
npm start
```

### Q2: 访问某些网站失败？

**可能原因：**
- 网站有反访问机制（验证码、IP 限制等）
- 网站需要登录才能访问
- 网络连接问题

**解决方法：**
- 尝试使用代理
- 手动在浏览器中打开，然后使用网页版智控台
- 检查网络连接

### Q3: 如何停止应用？

**方法：**
- 点击窗口右上角的关闭按钮（❌）
- 或在终端中按 `Ctrl + C`

### Q4: 导出的数据在哪里？

**答案：**
- 导出时会弹出对话框，让你选择保存位置
- 默认可以保存在「下载」文件夹或桌面

### Q5: 可以同时访问多个页面吗？

**答案：**
- 当前版本一次只能访问一个页面
- 可以打开多个 AI智控台窗口（不推荐）
- 建议逐个访问，然后合并数据

---

## 💡 最佳实践

### 1. 合法使用

- ✅ 遵守目标网站的服务条款
- ✅ 尊重 robots.txt 规则
- ✅ 不要访问敏感或隐私数据
- ✅ 控制访问频率，避免给服务器造成压力

### 2. 数据管理

- 及时导出重要数据
- 使用有意义的文件名（包含日期和主题）
- 定期整理和备份数据

### 3. 性能优化

- 避免同时访问大量页面
- 对于大型页面，可以分批次访问
- 定期清理缓存

### 4. 错误处理

- 如果访问失败，检查网址是否正确
- 查看控制台日志（开发模式下）
- 尝试使用网页版智控台作为替代

---

## 📞 技术支持

如有问题，请检查：

1. [README.md](README.md) - 详细的项目说明
2. [QUICKSTART.md](QUICKSTART.md) - 快速启动指南
3. 浏览器控制台日志（开发模式）

---

## 🎯 快速参考卡片

```
┌─────────────────────────────────────────────┐
│  ️ 无限空间·AI智控台 - 快速参考           │
├─────────────────────────────────────────────┤
│  启动：sh /Users/chincharles/Desktop/       │
│        myProgram/electron-crawler/start.sh  │
│                                             │
│  步骤：1. 输入网址                          │
│        2. 点击「开始访问」                  │
│        3. 查看数据                          │
│        4. 导出数据                          │
│                                             │
│  快捷键：Enter = 开始访问                   │
│                                             │
│  导出格式：JSON, CSV                        │
└─────────────────────────────────────────────┘
```

---

**祝你使用愉快！** 🎉

最后更新：2026-05-23
